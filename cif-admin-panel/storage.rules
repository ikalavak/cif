rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Allow read/write for local emulator testing
      allow read, write: if request.auth != null;
    }
  }
}